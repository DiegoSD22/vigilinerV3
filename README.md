# Vigiliner Backend

Backend API built with NestJS, Prisma and PostgreSQL using Docker.

## Requirements
- Docker
- Docker Compose

## Setup

```bash
git clone https://github.com/DiegoSD22/vigiliner.git
cd vigiliner-backend
cp .env.example .env
docker compose up --build
